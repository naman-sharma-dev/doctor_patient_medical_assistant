# Doctor-Patient Medical Assistant — Python dependencies
#
# Install all required dependencies:
#   pip install -r requirements.txt
#
# Optional — CMU Sphinx offline speech recognition:
#   pip install pocketsphinx

# speech_to_text.py
SpeechRecognition==3.15.2
pyaudio==0.2.14
